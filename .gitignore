# 密钥
.env
*.pem

# Python
__pycache__/
*.pyc
.venv/
venv/

# 数据（Archive dump、SQLite 库、爬取进度、封面图缓存 data/covers/）
data/
experiments/

# 本地 Claude Code 状态（settings.local.json 等，不进库）
.claude/

# 原型（抛弃式，不入库）
scripts/proto_*.py

# 打包产物（不入库）
*.tar.gz

# 发帖用截图（本地静态资源，不入库）
pics/
